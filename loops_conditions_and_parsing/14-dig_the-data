#!/bin/bash
# Parse the Apache log file and use awk to group visitors by IP and HTTP status code
# Display the data in the specified format and order it by the number of occurrences
awk '{print $1, $9}' apache-access.log | sort | uniq -c | sort -nr | awk '{print $2, $3, $1}' | head -n 10
