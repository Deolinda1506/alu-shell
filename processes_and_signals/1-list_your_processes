#!/usr/bin/env bash
# This script displays a list of currently running processes.

# Use the ps command to list all processes, include those without a TTY, and format the output.
# The -e option shows all processes, and -o defines the output format.
ps -e -o user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,command | head -50
