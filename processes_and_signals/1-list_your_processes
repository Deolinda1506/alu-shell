#!/usr/bin/env bash
# This script displays a list of currently running processes, showing only the USER column

# Print the header
echo "USER"

# Use the ps command to list processes and skip the header
ps -eo user --sort=user | grep -v "USER"
