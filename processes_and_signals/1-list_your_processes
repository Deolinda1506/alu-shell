#!/usr/bin/env bash
# This script displays a list of currently running processes

# Print the header
echo "USER STARTED COMMAND"

# Use the ps command to list processes, format the output, and skip the header
ps -eo user,lstart,cmd --sort=start_time | grep -v "COMMAND"
