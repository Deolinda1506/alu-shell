#!/bin/bash

# Assuming your log file is named "access.log"
LOG_FILE="access.log"

# Use awk to process the log file
awk '{count[$1" "$9]++} END {for (key in count) print count[key], key}' "$LOG_FILE" | sort -rn
